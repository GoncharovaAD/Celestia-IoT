version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

# Инструкция для установки зависимостей
python:
  install:
    - requirements: requirements.txt

# Если вы используете MkDocs, укажите это в блоке formats
mkdocs:
  configuration: mkdocs.yml
